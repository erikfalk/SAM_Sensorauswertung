#ifndef READING_H
#define READING_H


class Reading
{
public:
    Reading();
    int readFromCSV(QString filename);
    int readFromCzml(QString filename, QVector<SensorData>& readSensorData);
    
    //returns true if GPS Dataset Checksum is correct
    bool gpsChecksum(QString &dataline);
    
};

#endif // READING_H
